import { CancelToken } from 'axios';
import { SimpleJson } from '.';
export declare const fetchSpreadsheetV4: (sheetId: string, API_KEY: string, cancelToken: CancelToken | undefined) => Promise<Array<SimpleJson>>;
